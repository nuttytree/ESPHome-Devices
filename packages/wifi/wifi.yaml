wifi:
  ssid: ${wifi_ssid}
  password: ${wifi_password}
  fast_connect: true
  manual_ip:
    static_ip: ${ip_address}
    subnet: ${ip_subnet}
    gateway: ${ip_gateway}
  ap:
    ssid: ${device_id}
    password: ${ap_wifi_pwd}

#captive_portal: